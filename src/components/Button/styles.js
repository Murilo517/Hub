import styled from "styled-components";

export const Btn = styled.button`
  width: 55.49px;
  height: 32px;
  background: var(--grey-);
  border-radius: 4px;
  margin: 0 auto;
  color: var(--grey-0);
`;
